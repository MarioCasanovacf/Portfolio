# COMMIT_SUMMARY.md — first-commit readiness (Mario's commit human-gate)

Generated by harness-verifier-l for T-105 (monorepo-merge epic join). READ-ONLY: no
`git add`/`commit`/`push` was run to produce this. All evidence below was captured by
re-running commands from this repo's working tree on 2026-07-05, not copied from a
producer's note.

## 1. Build + path replay (adversarial re-check of T-103 / T-104 / T-122)

- `npm run build` from `Portfolio-repo/site` → **exit 0**, `12 page(s) built`.
  `find dist -name "*.html" | wc -l` → **12**. (No heston page yet — T-118, which adds
  it, is still `in_progress` on the blackboard; 12 is the correct current count, not a
  bug.)
- `notebooks/data/real/service_incidents.csv` and `github_actions_runs.csv` resolve at
  `01_professional/cloud_infrastructure_support/notebooks/data/real/` — T-104's move is
  intact.
- `01_professional/quantitative_finance/.github/dependabot.yml` → `directory:
  "/01_professional/quantitative_finance"` — the post-move path fix holds.
- `site/public/figures/hierarchical-risk-parity/` contains `dendrogram.svg`,
  `oos_comparison.svg`, `weights.svg` — T-122's recovered HRP figures land inside the
  repo.
- `Portfolio-repo/../site` (i.e. the old top-level `site/` outside `Portfolio-repo/`)
  does **not** exist — no dead tree left behind by the relocation.
- `git status --porcelain -uall | grep -E "node_modules|/dist/|\.astro/"` → **empty**.
  `.gitignore` covers `node_modules/`, `site/dist/`, `site/.astro/`, confirmed with
  `git check-ignore -v` on a sample `__pycache__` file too (also ignored correctly,
  including nested ones under `01_professional/review_sentiment_ml/`).
- No `.venv` tracked or staged; no file above ~2.5 MB in the full committable set;
  no `.env`/`*.pem`/`*.key`/credential-named file anywhere in the tracked+untracked set.
  `dbt_project/profiles.yml` uses `env_var()` for Snowflake creds (no hardcoded
  secrets); the new untracked `dbt_project/.user.yml` is just a dbt anonymous-usage
  UUID, not a secret.

**Verdict on this section: coherent, committable.**

## 2. What's already staged (index) — the reorg rename set

`git diff --cached --stat` → **189 files changed, 6015 insertions(+), 4859 deletions(-)**.
This is the `01_professional/` / `02_intellectual/` / `_pendiente_decision/` split from
T-104: almost entirely `R`/`RM` (rename, some with modification) moving each project's
files into its new home. Notable non-trivial diffs inside the rename set:
- `_pendiente_decision/real_estate/data/kc_house_data_synthetic.csv` (A) + the
  `house_sales_king_county.ipynb` restructuring — this is mid-flight from T-116
  (real_estate revival, still `in_progress` on the blackboard as of this check). It will
  keep changing; nothing to do here yet.
- Several `D` (plain delete) entries for the old un-prefixed paths
  (`cloud_infrastructure_support/notebooks/01..05_*.ipynb`,
  `cloud_infrastructure_support/src/data_generator.py`,
  `real_estate/house_sales_king_county.ipynb`,
  `subscription_economics/tests/unit/test_data_generator.py`) — these are the stale
  originals of files that were rewritten enough that git couldn't pair them as a rename;
  their content lives on at the new path. Confirm at commit time these aren't
  accidental content loss (the notebooks were rebuilt with real data per the fix
  campaign — this looks intentional, not a leftover).

## 3. What's newly untracked — what a commit should add

`git status --porcelain -uall` shows **39 top-level `??` entries** (some are
directories). The main groups:

- **`site/`** (the whole relocated Astro site, T-103) — `git ls-files --others
  --exclude-standard site/` → 55 files, ~1.1 MB. Verified none of it is
  `node_modules/`, `dist/`, or `.astro/` (gitignore working correctly on the new
  location too).
- **New research docs / notebooks that finished during this epic**:
  `01_professional/cloud_infrastructure_support/notebooks/` (the 5 rebuilt notebooks +
  `data/real/*.csv`), `.../research/` (preambulo/glossary/scientific_foundations +
  `notebook_redesign.md`), `.../src/capture_status_pages.py`,
  `.../src/fetch_gh_actions.py`.
- **Legislative pipeline fix (T-106/T-107/T-108)**: new dbt macros/models
  (`dipmex_date.sql`, `normalize_name.sql`, `v_legislator_attendance.sql`,
  `v_legislator_party_switches.sql`, `v_party_agreement.sql`, `v_party_cohesion.sql`,
  `stg_sitl_votes.sql`), `src/capture/sitl.py`, new ADRs (003, 004),
  `.../research/`, `.../scripts/`, `dbt_project/seeds/`, `dbt_project/tests/`.
- **02_intellectual additions**: `legislative_experiments/` (new notebook +
  rendered `.html`), source modules that were split out of notebooks
  (`rutherford.py`, `schrodinger.py`, `distances.py`, `ramachandran.py`) with their unit
  tests.
- **`_pendiente_decision/real_estate/`**: `data/real/` (the real Kaggle
  `kc_house_data.csv`, 2.5 MB — largest single file in the whole commit) and
  `revival_plan.md`, `src/spatial_diagnostics.py`, `tests/unit/test_pipeline.py` — this
  is T-115/T-116 work, **still in_progress**, will keep changing before it's final.
- **`01_professional/review_sentiment_ml/`** — new project directory (`app_ratings/` +
  `product_reviews/`), 1.6 MB. Grepped case-insensitively for the target brand across the whole
  directory: **zero hits**. Data files present are `synthetic_sample.csv` — consistent
  with Mario's memory note that this is the *scrubbed* migration target, not the leaky
  public repo. Flagged in section 4 for Mario's own confirmation regardless, since the
  memory note marks the underlying leak issue "URGENT" and unresolved.
- **`portfolio_style.py`**, `01_professional/README.md`, `02_intellectual/README.md`,
  `_pendiente_decision/README.md` — new top-level docs for the reorg.

## 4. Flags for Mario before committing

1. **`review_sentiment_ml/` — confirm scrubbed, not the leaky copy.** This verifier's
   grep for the target brand across the new directory found nothing, and the only data files are
   `synthetic_sample.csv`, which is consistent with a clean migration. But the memory
   note (`project_sentiment_ml_portfolio.md`) marks the underlying vendor-identifying
   leak as unresolved and urgent, and this verifier cannot fully audit model
   internals/notebook prose for indirect identification (e.g., a branded figure
   embedded as an image, or vendor names appearing in prose rather than data). Worth a
   deliberate look before this goes into a real commit, since a `git commit` is much
   harder to fully scrub later than a working-tree file.
2. **`_pendiente_decision/real_estate/` and the staged `kc_house_data_synthetic.csv` /
   `house_sales_king_county.ipynb` changes are mid-flight** (T-116 `in_progress`). If
   Mario wants one clean first commit rather than a half-finished revival baked in, wait
   for T-116/T-117 to close, or split it into a follow-up commit.
3. **The plain `D` deletions** listed in section 2 are old un-prefixed originals whose
   rewritten content now lives at the new path — worth Mario's own skim at commit time
   to confirm no unintended content loss, since this verifier's read is "looks
   intentional" rather than a line-by-line diff of every notebook.
4. Everything else (site relocation, dependabot fix, HRP figures, legislative pipeline
   fix artifacts, cloud notebooks) replayed clean and is safe to include as-is.

## 5. What should stay excluded (already handled correctly by `.gitignore`)

No changes needed — `node_modules/`, `site/dist/`, `site/.astro/`, `__pycache__/`,
`.venv/`, and `**/data/synthetic/*.csv` are all confirmed excluded from the
committable set by direct check (`git check-ignore -v`, and zero matches in
`git status --porcelain -uall | grep -E "node_modules|/dist/|\.astro/"`).
