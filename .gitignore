# Data files (too large for git)
**/data/synthetic/*.csv

# NDA — the sentiment project embeds real customer review text verbatim across
# its analysis reports AND its code (validation samples, highlights, stage-1
# diagnosis). Kept LOCAL until properly scrubbed. The public case study lives in
# site/ (which quotes no real reviews) and is unaffected.
01_professional/review_sentiment_ml/

# Mid-flight (T-116 real_estate revival) — excluded until the project settles.
_pendiente_decision/real_estate/

# Node / site (Astro)
node_modules/
site/dist/
site/.astro/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
dist/
build/
.env
.venv/
venv/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# OS
.DS_Store
Thumbs.db

# Claude Code
**/CLAUDE.md
.claude/
portfolio_preprompt.md
portfolio_gap_analysis.md
PRODUCTION_TEMPLATE.md
**/PLAN.md

# Internal handoff notes — never publish (NDA)
CLAUDE_TRANSITION_REPORT.md
*_TRANSITION_REPORT.md
