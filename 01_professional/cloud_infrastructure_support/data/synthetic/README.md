# Data

## Files

### `.gitkeep`
Placeholder file. This directory is intentionally empty in the repository.

## About

The `data/synthetic/` directory is populated at runtime by the project notebooks themselves. Synthetic data is generated inline during notebook execution rather than by a standalone data generator script.

No manual regeneration step is needed -- simply run the notebooks from the `notebooks/` directory and the required data will be created automatically.
